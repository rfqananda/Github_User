<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rifqiananda.githubuserv2">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GithubUserV2">
<!--        <activity android:name=".ui.themes.Theme"></activity>-->
        <activity
            android:name=".ui.main.MainActivity"
            android:theme="@style/Theme.GithubUserV2"
            android:windowSoftInputMode="adjustPan"></activity>
        <activity
            android:name=".ui.detail.DetailUser"
            android:theme="@style/Theme.GithubUserV2"></activity>
        <activity android:name=".ui.main.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>